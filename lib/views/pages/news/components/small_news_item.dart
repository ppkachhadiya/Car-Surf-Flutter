import 'package:flutter/material.dart';

class SmallNewItem extends StatelessWidget {
  const SmallNewItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
