import 'package:flutter/material.dart';

class BigNewItem extends StatelessWidget {
  const BigNewItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
